{ Sample program in EZ language -
  Program with semantic errors
}

program err;
var
    int x;
begin
    { SEMANTIC ERROR: Incompatible types for operator ':=',
      LHS is 'int' and RHS is 'string'. }
    x := "x = " + 42;
end
